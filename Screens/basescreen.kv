<BaseScreen>:
    name: 'base'

    BoxLayout:
        orientation: 'vertical'

        Label:
            color: 0, 0, 0, 1
            font_size: '14sp'
            text:
                # 'ttt'
                app.translation._('이 화면을 당신 것으로 바꾸세요.')